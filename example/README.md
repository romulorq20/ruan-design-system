# ruan_design_system_example

Exemplo de uso do Ruan Design System. Projeto focado em **mobile** (Android e iOS).

## Executar

```bash
flutter pub get
flutter run
```

Para escolher o dispositivo: `flutter run -d <device_id>` (use `flutter devices` para listar).
